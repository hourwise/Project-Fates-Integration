# Handoffs

Place per-repository handoff packets in this directory.

Each handoff should follow the schema defined in `schemas/handoff.schema.json`.

See `handoff.example.json` for the expected format.
